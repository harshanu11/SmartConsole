namespace ValueTypeTest
{
    public struct StructTest
    {
        public StructTest(double x, double y)
        {
            X = x;
            Y = y;
        }

        public double X { get; }
        public double Y { get; }

        public override string ToString() => $"({X}, {Y})";
    }
}
